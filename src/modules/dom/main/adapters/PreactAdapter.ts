// TODO

/*
import Preact from 'preact'

import Adapter from '../types/Adapter'

const PreactAdapter: Adapter = {
  createElement: Preact.h,
  createContext: Preact.createContext,
  forwardRef: Preact.forwardRef,

  useContext: Preact.useContext,
  useEffect: react.useEffect,
  useState: useState,
  useMethods: useImperativeMethods,

  mount: Preact.render,
  unmount: (container: any) => Preact.render(null, container),

  Fragment: Preact.Fragment
}

export default PreactAdapter
*/