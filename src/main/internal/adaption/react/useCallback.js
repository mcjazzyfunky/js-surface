// external import
import React from 'react'

// --- useCallback --------------------------------------------------

const useCallback = React.useCallback

// --- exports ------------------------------------------------------

export default useCallback
