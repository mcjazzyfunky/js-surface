// external import
import Preact from 'preact'

// --- isValidElement -----------------------------------------------

const isValidElement = Preact.isValidElement

// --- exports ------------------------------------------------------

export default isValidElement
