// external import 
import Preact from 'preact'

// --- render -------------------------------------------------------

const unmuntComponentAtNode = Preact.render.bind(null, null)

// --- exports ------------------------------------------------------

export default unmuntComponentAtNode
